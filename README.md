# odii-sale-channel-service

### How to run

- Create a `.env` file following the `.env.example` or export these variables to the environment

- Run `npm start` to start the application, it will be running on port 3000 by default

### TODO
